# 10.3 Practice — File I/O

[[10 - File IO/10.2 Processing File Data|← 10.2 Processing File Data]] | [[Home|🏠 Home]]

---

## Exercise 1 — Word Counter

Write a program that reads a file of text and counts the total number of words.

```run-java
import java.util.Scanner;
import java.io.File;
import java.io.FileWriter;

public class Main {
    public static void main(String[] args) throws Exception {
        // Create sample file
        FileWriter fw = new FileWriter("passage.txt");
        fw.write("To be or not to be that is the question\n");
        fw.write("Whether tis nobler in the mind to suffer\n");
        fw.write("The slings and arrows of outrageous fortune\n");
        fw.close();

        // Your code here — count total words

    }
}
```

---

## Exercise 2 — Grade Report

Read a file of `name score` pairs and print a report showing each student's name, score, and letter grade. Also print the class average.

```run-java
import java.util.Scanner;
import java.io.File;
import java.io.FileWriter;

public class Main {
    public static void main(String[] args) throws Exception {
        FileWriter fw = new FileWriter("classgrades.txt");
        fw.write("Alice 92\nBob 75\nCarol 88\nDave 61\nEve 95\nFrank 83\n");
        fw.close();

        // Your code here

    }
}
```

---

## Exercise 3 — File Search

Read a file of words (one per line) and print all words that contain a specific substring. Ask the user for the search term.

```run-java
import java.util.Scanner;
import java.io.File;
import java.io.FileWriter;

public class Main {
    public static void main(String[] args) throws Exception {
        FileWriter fw = new FileWriter("dictionary.txt");
        fw.write("apple\napplication\napricot\nbanana\nblueberry\nblackcurrant\ncherry\n");
        fw.close();

        Scanner keyboard = new Scanner(System.in);
        System.out.print("Search for: ");
        String query = keyboard.next();

        // Your code here — print all words containing query

    }
}
```

---

## AP-Style Multiple Choice

> [!QUESTION] Question 1
> Which of the following correctly reads all integers from a file using a Scanner?
>
> - A)
> ```java
> while (sc.hasNextLine()) {
>     int n = sc.nextInt();
>     process(n);
> }
> ```
> - B)
> ```java
> while (sc.hasNextInt()) {
>     int n = sc.nextInt();
>     process(n);
> }
> ```
> - C)
> ```java
> while (sc.hasNext()) {
>     int n = sc.nextInt();
>     process(n);
> }
> ```
> - D) Both B and C are correct
>
> **Your answer:**
>
> **Explain the difference:**

---

> [!QUESTION] Question 2
> A student writes:
> ```java
> Scanner sc = new Scanner(new File("data.txt"));
> ```
> Without a `throws` clause or try-catch, what happens?
>
> - A) The code runs fine — Scanner handles it internally
> - B) The code throws a RuntimeException at runtime
> - C) The code won't compile — FileNotFoundException must be handled
> - D) The file is created if it doesn't exist
>
> **Your answer:**

---

## Challenge — GPA Calculator

A file contains student records, one per line, formatted as:
`LastName FirstName grade1 grade2 grade3 grade4`

Write a program that:
1. Reads every student from the file
2. Computes each student's average grade
3. Prints a ranked list from highest to lowest average

```run-java
import java.util.Scanner;
import java.util.ArrayList;
import java.io.File;
import java.io.FileWriter;

public class Main {
    public static void main(String[] args) throws Exception {
        FileWriter fw = new FileWriter("roster.txt");
        fw.write("Smith Alice 88 92 79 95\n");
        fw.write("Jones Bob 72 68 81 75\n");
        fw.write("Chen Carol 95 98 91 97\n");
        fw.write("Davis Dave 60 72 65 70\n");
        fw.close();

        // Your code here — read, compute averages, sort and print

    }
}
```

---

[[10 - File IO/10.2 Processing File Data|← 10.2 Processing File Data]] | [[Home|🏠 Home]]
