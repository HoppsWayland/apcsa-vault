# 4.6 Practice — Control Flow

[[04 - Control Flow/4.5 Nested Loops|← 4.5 Nested Loops]] | [[Home|🏠 Home]]

---

## Exercise 1 — FizzBuzz

The classic. Print the numbers 1 to 100, but:
- For multiples of 3, print `"Fizz"` instead
- For multiples of 5, print `"Buzz"` instead
- For multiples of both 3 and 5, print `"FizzBuzz"`

```run-java
public class Main {
    public static void main(String[] args) {
        // Your code here

    }
}
```

---

## Exercise 2 — Number Guessing Game

Write a guessing game where:
1. A random number between 1 and 100 is generated
2. The user keeps guessing until they get it right
3. After each guess, print "Too high", "Too low", or "Correct!"
4. After the game, print how many guesses it took

```run-java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        // Your code here

    }
}
```

---

## Exercise 3 — Prime Checker

Write a program that asks the user for a number and prints whether it is prime. A prime number is only divisible by 1 and itself.

*Hint: use a loop to check divisibility from 2 up to the square root of the number.*

```run-java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        // Your code here

    }
}
```

---

## Exercise 4 — String Reversal

Without using any built-in reverse method, write a loop that reverses a String.

```run-java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a word: ");
        String word = sc.next();

        // Your code here — build the reversed String character by character

    }
}
```

---

## AP-Style Multiple Choice

> [!QUESTION] Question 1
> What is printed by the following?
> ```java
> int x = 1;
> while (x < 100) {
>     x *= 2;
> }
> System.out.println(x);
> ```
> - A) 64
> - B) 100
> - C) 128
> - D) 256
>
> **Your answer:**
>
> **Trace the loop (show each value of x):**

---

> [!QUESTION] Question 2
> How many times is `"Hello"` printed?
> ```java
> for (int i = 0; i < 5; i++) {
>     for (int j = i; j < 5; j++) {
>         System.out.println("Hello");
>     }
> }
> ```
> - A) 10
> - B) 15
> - C) 20
> - D) 25
>
> **Your answer:**
>
> **Explain:**

---

> [!QUESTION] Question 3
> Which of the following is equivalent to `!(x > 5 && y < 10)`?
>
> - A) `x < 5 && y > 10`
> - B) `x <= 5 || y >= 10`
> - C) `x > 5 || y < 10`
> - D) `x <= 5 && y >= 10`
>
> **Your answer:**
>
> **Apply De Morgan's Law step by step:**

---

## Challenge — Diamond Pattern

Print a diamond of stars of size `n` (user input). For `n = 3`:
```
  *
 ***
*****
 ***
  *
```

*Hint: solve the top half first (it's a triangle with leading spaces), then the bottom half.*

```run-java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter n: ");
        int n = sc.nextInt();

        // Your code here

    }
}
```

---

[[04 - Control Flow/4.5 Nested Loops|← 4.5 Nested Loops]] | [[Home|🏠 Home]]
