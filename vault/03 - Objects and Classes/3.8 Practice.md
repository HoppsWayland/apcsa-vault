# 3.8 Practice — Objects and Classes

[[03 - Objects and Classes/3.7 Scope and Access|← 3.7 Scope and Access]] | [[Home|🏠 Home]]

---

## Exercise 1 — Book Class

Design and implement a `Book` class with:
- Instance variables: `title` (String), `author` (String), `pages` (int), `isCheckedOut` (boolean)
- A constructor that takes title, author, and pages (isCheckedOut starts as false)
- Getters for all fields
- Methods: `checkOut()` and `returnBook()` that update isCheckedOut
- A `toString()` that returns something like: `"The Great Gatsby by F. Scott Fitzgerald (180 pages)"`

Test it by creating two books, checking one out, and printing both.

```run-java
public class Main {

    static class Book {
        // Your instance variables here

        // Your constructor here

        // Your methods here

    }

    public static void main(String[] args) {
        // Test your Book class here

    }
}
```

---

## Exercise 2 — Rectangle Class

Build a `Rectangle` class with:
- Instance variables: `width` and `height` (both double)
- A constructor
- Getters and setters (setters should reject non-positive values)
- `getArea()` and `getPerimeter()` methods
- `isSquare()` — returns true if width equals height
- `toString()`

```run-java
public class Main {

    static class Rectangle {
        // Your code here

    }

    public static void main(String[] args) {
        // Create rectangles and test all your methods

    }
}
```

---

## Exercise 3 — Using Objects Together

Create a `Student` class (name, gradeLevel, gpa) and a `Classroom` class (teacherName, and three Student objects as instance variables). Give `Classroom` a method `printRoster()` that prints the teacher and all three students.

```run-java
public class Main {

    static class Student {
        // Your code here

    }

    static class Classroom {
        // Your code here

    }

    public static void main(String[] args) {
        // Your code here

    }
}
```

---

## AP-Style Multiple Choice

> [!QUESTION] Question 1
> Consider this class:
> ```java
> public class Counter {
>     private int count;
>
>     public Counter() {
>         count = 0;
>     }
>
>     public void increment() {
>         count++;
>     }
>
>     public int getCount() {
>         return count;
>     }
> }
> ```
> What is printed by the following?
> ```java
> Counter c = new Counter();
> c.increment();
> c.increment();
> c.increment();
> System.out.println(c.getCount());
> ```
> - A) 0
> - B) 1
> - C) 2
> - D) 3
>
> **Your answer:**

---

> [!QUESTION] Question 2
> Which of the following best describes encapsulation?
>
> - A) A class that extends another class
> - B) Hiding instance variables and exposing controlled access through methods
> - C) A method that calls itself
> - D) Using the same method name for different parameter types
>
> **Your answer:**

---

> [!QUESTION] Question 3
> A class has this constructor:
> ```java
> public Dog(String name, int age) {
>     this.name = name;
>     this.age  = age;
> }
> ```
> What is the purpose of `this.name` vs `name`?
>
> - A) `this.name` refers to the parameter; `name` refers to the instance variable
> - B) `this.name` refers to the instance variable; `name` refers to the parameter
> - C) They are identical — `this` is optional
> - D) `this.name` is a static variable shared by all Dog objects
>
> **Your answer:**

---

## Challenge — Vending Machine

Design a `VendingMachine` class that:
- Holds a product name, price (double), and quantity (int)
- Has a method `purchase(double payment)` that:
  - Returns the change if payment is sufficient and quantity > 0
  - Returns -1 if payment is insufficient
  - Returns -2 if out of stock
- Decrements quantity on a successful purchase
- Has a `toString()`

```run-java
public class Main {

    static class VendingMachine {
        // Your code here

    }

    public static void main(String[] args) {
        // Test all three scenarios: success, insufficient payment, out of stock

    }
}
```

---

[[03 - Objects and Classes/3.7 Scope and Access|← 3.7 Scope and Access]] | [[Home|🏠 Home]]
