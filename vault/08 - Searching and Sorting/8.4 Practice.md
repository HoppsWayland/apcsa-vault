# 8.4 Practice — Searching and Sorting

[[08 - Searching and Sorting/8.3 Insertion Sort|← 8.3 Insertion Sort]] | [[Home|🏠 Home]]

---

## Exercise 1 — Search Variations

Modify linear search to return **all** indices where the target appears (not just the first).

```run-java
import java.util.ArrayList;

public class Main {

    static ArrayList<Integer> findAll(int[] arr, int target) {
        // Your code here — return a list of all matching indices

        return new ArrayList<>();
    }

    public static void main(String[] args) {
        int[] nums = {3, 7, 2, 7, 5, 7, 1};
        System.out.println(findAll(nums, 7));   // [1, 3, 5]
        System.out.println(findAll(nums, 9));   // []
    }
}
```

---

## Exercise 2 — Sort Strings

Adapt selection sort to sort an array of Strings alphabetically.

*Hint: use `.compareTo()` instead of `<`.*

```run-java
public class Main {

    static void sortStrings(String[] arr) {
        // Your code here

    }

    public static void main(String[] args) {
        String[] words = {"banana", "apple", "date", "cherry", "elderberry"};
        sortStrings(words);
        for (String w : words) System.out.print(w + " ");
    }
}
```

---

## Exercise 3 — Binary Search on Strings

Implement binary search for a sorted array of Strings.

```run-java
public class Main {

    static int binarySearchStrings(String[] arr, String target) {
        // Your code here

        return -1;
    }

    public static void main(String[] args) {
        String[] words = {"apple", "banana", "cherry", "date", "elderberry", "fig"};
        System.out.println(binarySearchStrings(words, "cherry"));      // 2
        System.out.println(binarySearchStrings(words, "grape"));       // -1
    }
}
```

---

## AP-Style Multiple Choice

> [!QUESTION] Question 1
> What is the maximum number of comparisons needed to find a value in a sorted array of 128 elements using binary search?
>
> - A) 7
> - B) 8
> - C) 64
> - D) 128
>
> **Your answer:**
>
> **Explain:**

---

> [!QUESTION] Question 2
> After two complete passes of selection sort on `{7, 4, 2, 9, 1, 5}`, what does the array look like?
>
> - A) `{1, 2, 7, 4, 9, 5}`
> - B) `{1, 2, 4, 7, 9, 5}`
> - C) `{1, 4, 2, 9, 7, 5}`
> - D) `{1, 2, 9, 7, 4, 5}`
>
> **Your answer:**
>
> **Trace both passes:**

---

> [!QUESTION] Question 3
> Which of the following is true about insertion sort vs selection sort?
>
> - A) Insertion sort always makes fewer comparisons than selection sort
> - B) Selection sort requires more swaps than insertion sort on average
> - C) Insertion sort can be faster than selection sort when the input is nearly sorted
> - D) Both algorithms have the same performance on all inputs
>
> **Your answer:**

---

## Challenge — Sort an ArrayList of Objects

Sort an `ArrayList` of `Student` objects (name, gpa) by GPA in descending order, using insertion sort.

```run-java
import java.util.ArrayList;

public class Main {

    static class Student {
        String name;
        double gpa;

        Student(String name, double gpa) {
            this.name = name;
            this.gpa  = gpa;
        }

        public String toString() {
            return name + "(" + gpa + ")";
        }
    }

    static void sortByGpa(ArrayList<Student> students) {
        // Your code here — insertion sort, descending GPA

    }

    public static void main(String[] args) {
        ArrayList<Student> students = new ArrayList<>();
        students.add(new Student("Alice", 3.8));
        students.add(new Student("Bob",   3.2));
        students.add(new Student("Carol", 3.9));
        students.add(new Student("Dave",  3.5));

        sortByGpa(students);
        System.out.println(students);
        // Expected: [Carol(3.9), Alice(3.8), Dave(3.5), Bob(3.2)]
    }
}
```

---

[[08 - Searching and Sorting/8.3 Insertion Sort|← 8.3 Insertion Sort]] | [[Home|🏠 Home]]
